# DataTableV2 Demo

本地启动：

```bash
npm run demo:datatablev2
```

默认地址：

```text
http://localhost:5174
```

可选端口：

```bash
PORT=5180 npm run demo:datatablev2
```

这个 demo 用来测试 `src/DataTableV2` 的展示和交互状态，包含标准数据、高密度数据、宽表横滚、长文本溢出、空数据、加载中等场景。页面左侧可以切换分页、全选菜单、已选清空条和模拟加载，也可以快速选择当前页、选择全部可选行、清空选择、重置列设置。
